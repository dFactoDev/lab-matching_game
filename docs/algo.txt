
Main():
	cardIcons = [icon1, icon2, icon3, ... ]
	Deck = ShuffleDeck(cardIcons) // create new deck with cards randomly assinged
	layoutDeck(Deck)
	cardsClicked = []
	matchCount = 0
	moveCount = 0
	
	if (click on card):
		add card to cardsClicked
		if (cardsClicked = NULL): //no other card currently open
			show card
		else: // other card already open
			moveCount += 1
			show card
			if (cardsClicked[0] == cardsClicked[1])
				matchCount += 1
				for (i in cardsClicked):
					open card //lock in open position
				if (matchCount == 6):
					endGame();
			else:
				for (i in cardsClicked):
					close card
	
layoutDeck(array Deck, array ul):
	for i in Deck:
		for li in ul:
			ul.addClass[i]
			
		
	
function ShuffleDeck(array) {
	  var m = array.length, t, i;

	  // While there remain elements to shuffle…
	  while (m) {

		// Pick a remaining element…
		i = Math.floor(Math.random() * m--);

		// And swap it with the current element.
		t = array[m];
		array[m] = array[i];
		array[i] = t;
	  }

	  return array;
	}
						
					
				
		
	